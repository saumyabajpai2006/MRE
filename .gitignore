/__pycache__
*.pyc

# Environment secrets (never commit .env, but .env.example is tracked)
.env
.env.local
.env.*.local

# Exclude large joblib files from git
*.joblib
!recommender/models/vectorizer.joblib
# Prevent only similarity.joblib from being committed (too large for Render)
recommender/models/similarity.joblib

# Allow parquet data files
!recommender/models/*.parquet